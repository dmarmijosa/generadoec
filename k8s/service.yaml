apiVersion: v1
kind: Service
metadata:
  name: generadorec-service
  namespace: generadorec
  labels:
    app: generadorec
    component: app
spec:
  type: ClusterIP
  selector:
    app: generadorec
    component: app
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
