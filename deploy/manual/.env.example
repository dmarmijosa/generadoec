# Copia este archivo como .env y completa los valores requeridos
# Token del túnel de Cloudflare (modo token)
TUNNEL_TOKEN=

# Si prefieres modo declarativo (sin token), ignora TUNNEL_TOKEN y utiliza ./cloudflared/config.yml
# Asegúrate de colocar el archivo de credenciales JSON en deploy/manual/cloudflared/generadorec-tunnel.json
